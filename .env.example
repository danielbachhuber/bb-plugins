# Copy to .env to override. The stories render bb's own components and styles,
# so they need a bb checkout with `pnpm install` already run in it.
BB_SOURCE_DIR=~/projects/bb

# Where `npm run screenshots` writes and commits story images. Defaults to
# ../bb-plugins-screenshots.
# BB_PLUGINS_SCREENSHOTS_DIR=~/projects/bb-plugins-screenshots
